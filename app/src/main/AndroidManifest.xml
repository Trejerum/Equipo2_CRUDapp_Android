<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.equipo2_crudapp_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_ofertapps"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_ofertapps_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShopsActivity" />
        <activity android:name=".AddSoftwareActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".MainActivity"/>
    </application>

</manifest>